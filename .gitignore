.vscode/
*.log
*.tmp

# Script to sync mod files over to the documents mod directory so the game can actually use them.
mod-sync.sh

# Merged output files
paradox-file-utils/vanilla-synchronizer/sync-output/
paradox-file-utils/parser/.antlr